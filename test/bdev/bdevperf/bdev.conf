[QoS]
  # QoS section defines limitation on performance
  # metric like IOPS and bandwidth
  #
  # Format: Limit_IOPS Bdev_Name IOPS_Limit_Value
  #
  # IOPS limit must be 10000 or greater and be multiple
  # of 10000
  #
  # Assign 20000 IOPS for the Malloc0 block device
  Limit_IOPS Malloc1 20000

[Split]
  # Split Malloc1 into two auto-sized halves
  Split Malloc0 2
  Split Malloc0p0 3
  Split Malloc0p0p1 1
  
[Malloc]
  NumberOfLuns 3
  LunSizeInMB 32

#[Nvme]
#  TransportID "trtype:PCIe traddr:0000:3a:00.0" Nvme0

# study all log outputs
# sudo ./bdevperf -c bdev.conf -q 1 -o 512 -w read -t 1 -T Malloc0 -L all

# simple IO
# sudo ./bdevperf -c bdev.conf -q 1 -o 4096 -w read -t 1 -T Malloc0

# deeper bdev stack, cost of split
# sudo ./bdevperf -c bdev.conf -q 1 -o 4096 -w write -t 1 -T Malloc2
# sudo ./bdevperf -c bdev.conf -q 1 -o 4096 -w write -t 1 -T Malloc0p1
# sudo ./bdevperf -c bdev.conf -q 1 -o 4096 -w write -t 1 -T Malloc0p0p2
# sudo ./bdevperf -c bdev.conf -q 1 -o 4096 -w write -t 1 -T Malloc0p0p1p0

# 4 core, need 4 Luns
# sudo ./bdevperf -c bdev.conf -q 128 -o 4096 -w read -t 1 -m f

# TODO:
# local thread to func direct call
# event => msg
